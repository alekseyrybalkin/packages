pkgname=python-idna
pkgver=2.9
vcs=git
vcs_pkgname=idna
gittag=v${pkgver}
relmon_id=5487

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
