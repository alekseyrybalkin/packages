pkgname=python-moderngl-window
SKIP_ARCH_CHECK=1
pkgver=1.2.0
vcs=git
vcs_pkgname=moderngl_window
gittag=${pkgver}

build() {
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir}
}
