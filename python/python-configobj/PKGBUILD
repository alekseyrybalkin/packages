pkgname=python-configobj
pkgver=5.0.6
vcs=git
vcs_pkgname=configobj
gittag=v${pkgver}
relmon_id=3809

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
