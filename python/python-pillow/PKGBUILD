pkgname=python-pillow
pkgver=7.2.0
vcs=git
vcs_pkgname=pillow
gittag=${pkgver}
relmon_id=7974

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
