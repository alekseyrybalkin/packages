pkgname=python-pyrsistent
pkgver=0.16.0
vcs=git
vcs_pkgname=pyrsistent
gittag=v${pkgver}
relmon_id=19676

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
