pkgname=python-pygit2
pkgver=1.0.2
vcs=git
vcs_pkgname=pygit2
gittag=v${pkgver}
relmon_id=3985

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
