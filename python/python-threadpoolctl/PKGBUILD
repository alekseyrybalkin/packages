pkgname=python-threadpoolctl
pkgver=2.1.0
vcs=git
vcs_pkgname=threadpoolctl
gittag=${pkgver}
relmon_id=52302

build() {
    cp ../setup.py .
    rm pyproject.toml
    python-build
}

package() {
    python-package
    rm -rf ${pkgdir}/usr/lib/python*/site-packages/tests/
}
