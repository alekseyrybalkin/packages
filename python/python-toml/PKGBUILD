pkgname=python-toml
pkgver=0.10.1
vcs=git
vcs_pkgname=toml
gittag=${pkgver}
relmon_id=6883

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
