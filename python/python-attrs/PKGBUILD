pkgname=python-attrs
pkgver=19.3.0
vcs=git
vcs_pkgname=attrs
gittag=${pkgver}
relmon_id=15099

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
