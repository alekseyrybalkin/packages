pkgname=python-pywavefront
SKIP_ARCH_CHECK=1
pkgver=1.1.0
vcs=git
vcs_pkgname=pywavefront
gittag=${pkgver}

build() {
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir}
}
