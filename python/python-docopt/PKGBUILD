pkgname=python-docopt
pkgver=0.6.2
vcs=git
vcs_pkgname=docopt
gittag=${pkgver}
relmon_id=19769

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
