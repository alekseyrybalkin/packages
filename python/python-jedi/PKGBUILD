pkgname=python-jedi
pkgver=0.17.0
vcs=git
vcs_pkgname=jedi
gittag=v${pkgver}
relmon_id=3893

build() {
    git clone `find_vcs_repo typeshed` jedi/third_party/typeshed
    git submodule update --init

    python-build
}

package() {
    python-package
}
