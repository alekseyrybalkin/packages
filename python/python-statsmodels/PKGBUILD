pkgname=python-statsmodels
pkgver=0.11.1
vcs=git
vcs_pkgname=statsmodels
gittag=v${pkgver}
relmon_id=4039

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
