pkgname=python-bottleneck
pkgver=1.3.2
vcs=git
vcs_pkgname=bottleneck
gittag=v${pkgver}
relmon_id=3787

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
