pkgname=python-texttable
pkgver=1.6.2
vcs=git
vcs_pkgname=texttable
gittag=v${pkgver}
relmon_id=11635

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
