pkgname=python-click
pkgver=7.1.2
vcs=git
vcs_pkgname=click
gittag=${pkgver}
relmon_id=3802

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
