pkgname=python-click
pkgver=7.1.2
vcs=git
vcs_pkgname=click
gittag=${pkgver}
relmon_id=3802

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
