pkgname=python-more-itertools
pkgver=7.2.0
vcs=git
vcs_pkgname=more-itertools
gittag=v${pkgver}
relmon_id=12201

build() {
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir} --optimize=1
}
