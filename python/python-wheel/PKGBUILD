pkgname=python-wheel
pkgver=0.34.2
vcs=git
vcs_pkgname=wheel
hgtag=${pkgver}
relmon_id=11428

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
