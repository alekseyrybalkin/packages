pkgname=python-patsy
pkgver=0.5.1
vcs=git
vcs_pkgname=patsy
gittag=v${pkgver}
relmon_id=3958

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
