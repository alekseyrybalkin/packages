pkgname=python-pyasn1
pkgver=0.4.8
vcs=git
vcs_pkgname=pyasn1
gittag=v${pkgver}
relmon_id=6996

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
