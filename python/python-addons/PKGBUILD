pkgname=python-addons
pkgver=1
vcs=git

build() {
    :
}

package() {
    python-package
}

after_install() {
    mkdir -pv /var/lib/jinni/{installed,uninstalled}
}

after_upgrade() {
    after_install
}
