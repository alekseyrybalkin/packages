pkgname=python-flask
pkgver=1.1.1
vcs=git
vcs_pkgname=flask
gittag=${pkgver}
relmon_id=3867

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
