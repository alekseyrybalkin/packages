pkgname=python-flask
pkgver=1.1.2
vcs=git
vcs_pkgname=flask
gittag=${pkgver}
relmon_id=3867

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
