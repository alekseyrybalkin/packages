pkgname=python-wcwidth
pkgver=0.2.3
vcs=git
vcs_pkgname=wcwidth
gittag=${pkgver}
relmon_id=8743

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
