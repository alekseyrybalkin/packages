pkgname=python-onetimepass
SKIP_ARCH_CHECK=1
pkgver=1.0.1
vcs=git
vcs_pkgname=onetimepass
gittag=v${pkgver}

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
