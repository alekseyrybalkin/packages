pkgname=python-backcall
pkgver=0.1
vcs=git
vcs_pkgname=backcall
gittag=${pkgver}
relmon_id=19636

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
