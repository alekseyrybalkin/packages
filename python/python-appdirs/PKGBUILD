pkgname=python-appdirs
pkgver=1.4.4
vcs=git
vcs_pkgname=appdirs
gittag=${pkgver}
relmon_id=6278

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
