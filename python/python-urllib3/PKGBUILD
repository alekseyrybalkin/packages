pkgname=python-urllib3
pkgver=1.25.9
vcs=git
vcs_pkgname=urllib3
gittag=${pkgver}
relmon_id=4078

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
