pkgname=python-dateutil
pkgver=2.8.1
vcs=git
vcs_pkgname=dateutil
gittag=${pkgver}
relmon_id=5621

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
