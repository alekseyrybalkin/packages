pkgname=python-paramiko
pkgver=2.7.0
vcs=git
vcs_pkgname=paramiko
gittag=${pkgver}
relmon_id=3954

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
