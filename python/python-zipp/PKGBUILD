pkgname=python-zipp
pkgver=3.1.0
vcs=git
vcs_pkgname=zipp
gittag=v${pkgver}

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
