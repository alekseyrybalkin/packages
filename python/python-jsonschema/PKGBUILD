pkgname=python-jsonschema
pkgver=3.2.0
vcs=git
vcs_pkgname=jsonschema
gittag=v${pkgver}
relmon_id=3898

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
