pkgname=python-gevent
pkgver=20.04.0
vcs=git
vcs_pkgname=gevent
gittag=${pkgver}
relmon_id=6835

build() {
    EMBED=1 \
    CYTHON=cython \
    PYTHON=python \
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir}
}
