pkgname=python-gevent
pkgver=20.6.1
vcs=git
vcs_pkgname=gevent
gittag=${pkgver}
relmon_id=6835

build() {
    python-build
}

package() {
    export EMBED=1
    export CYTHON=cython
    export PYTHON=python
    python-package
}
