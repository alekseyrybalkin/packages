pkgname=python-gevent
pkgver=20.5.2
vcs=git
vcs_pkgname=gevent
gittag=${pkgver}
relmon_id=6835

build() {
    EMBED=1 \
    CYTHON=cython \
    PYTHON=python \
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir}
}
