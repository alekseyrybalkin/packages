pkgname=python-cryptography
pkgver=2.9.2
vcs=git
vcs_pkgname=cryptography
gittag=${pkgver}
relmon_id=5533

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
