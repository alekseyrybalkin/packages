pkgname=python-cryptography
pkgver=2.9.2
vcs=git
vcs_pkgname=cryptography
gittag=${pkgver}
relmon_id=5533

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
