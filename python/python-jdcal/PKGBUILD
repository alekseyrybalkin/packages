pkgname=python-jdcal
pkgver=1.4.1
vcs=git
vcs_pkgname=jdcal
gittag=v${pkgver}
relmon_id=7978

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
