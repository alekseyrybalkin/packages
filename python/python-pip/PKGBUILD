pkgname=python-pip
pkgver=20.1.1
vcs=git
vcs_pkgname=pip
gittag=${pkgver}
relmon_id=6529

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir}
}
