pkgname=python-pip
pkgver=20.1.1
vcs=git
vcs_pkgname=pip
gittag=${pkgver}
relmon_id=6529

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
