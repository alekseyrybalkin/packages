pkgname=python-jinja
pkgver=2.11.2
vcs=git
vcs_pkgname=jinja
gittag=${pkgver}
relmon_id=3894

build() {
    patch -Np1 -i ../sphinx-remove-pallets.diff
    python-build
}

package() {
    python-package
}
