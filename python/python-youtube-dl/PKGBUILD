pkgname=python-youtube-dl
pkgver=2020.03.24
vcs=git
vcs_pkgname=youtube-dl
gittag=${pkgver}
relmon_id=5292

build() {
    :
}

package() {
    pip install --no-deps --ignore-installed --prefix=/usr --root=${pkgdir} .
    rm -rf ${pkgdir}/usr/etc
}
