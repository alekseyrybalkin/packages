pkgname=python-youtube-dl
pkgver=2020.05.29
vcs=git
vcs_pkgname=youtube-dl
gittag=${pkgver}
relmon_id=5292

build() {
    python-build
}

package() {
    python-package

    rm -rf ${pkgdir}/usr/etc
}
