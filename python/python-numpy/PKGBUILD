pkgname=python-numpy
pkgver=1.18.5
vcs=git
vcs_pkgname=numpy
gittag=v${pkgver}
relmon_id=2509

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
