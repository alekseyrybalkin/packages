pkgname=python-docker
pkgver=4.2.1
vcs=git
vcs_pkgname=docker-py
gittag=${pkgver}
relmon_id=3847

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
