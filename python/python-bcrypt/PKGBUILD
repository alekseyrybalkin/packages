pkgname=python-bcrypt
pkgver=3.1.7
vcs=git
vcs_pkgname=bcrypt
gittag=${pkgver}
relmon_id=9047

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
