pkgname=python-docs-theme
pkgver=2020.1
vcs=git
# e7367ebe35c3d6622d7e2bb01d7881e476eeadf0
gittag=jinni-${pkgver}

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
