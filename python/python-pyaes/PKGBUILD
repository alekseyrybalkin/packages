pkgname=python-pyaes
pkgver=1.6.1
vcs=git
vcs_pkgname=pyaes
gittag=v${pkgver}
relmon_id=14010

build() {
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir} --optimize=1
}
