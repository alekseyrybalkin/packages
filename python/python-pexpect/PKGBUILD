pkgname=python-pexpect
pkgver=4.8.0
vcs=git
vcs_pkgname=pexpect
gittag=${pkgver}
relmon_id=13058

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
