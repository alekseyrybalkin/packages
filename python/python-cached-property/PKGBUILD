pkgname=python-cached-property
pkgver=1.5.1
vcs=git
vcs_pkgname=cached-property
gittag=${pkgver}
relmon_id=6048

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
