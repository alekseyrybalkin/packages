pkgname=python-kiwisolver
pkgver=1.2.0
vcs=git
vcs_pkgname=kiwi
gittag=${pkgver}
relmon_id=16910

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
