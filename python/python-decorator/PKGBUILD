pkgname=python-decorator
pkgver=4.4.2
vcs=git
vcs_pkgname=decorator
gittag=${pkgver}
relmon_id=3819

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
