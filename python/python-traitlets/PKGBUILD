pkgname=python-traitlets
pkgver=4.3.3
vcs=git
vcs_pkgname=traitlets
gittag=${pkgver}
relmon_id=12211

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
