pkgname=python-yaml
pkgver=5.3.1
vcs=git
vcs_pkgname=pyyaml
gittag=${pkgver}
relmon_id=4123

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
