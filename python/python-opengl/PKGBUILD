pkgname=python-opengl
pkgver=3.1.5
vcs=git
vcs_pkgname=pyopengl
# 3e9791ffb4cd4831dae261d6bea3049ce9e78f01
gittag=jinni-${pkgver}
relmon_id=3754

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
