pkgname=python-opengl
pkgver=3.1.5
vcs=git
vcs_pkgname=pyopengl
gittag=jinni-${pkgver}
relmon_id=3754

build() {
    python setup.py build
}

package() {
    python setup.py install --root=${pkgdir}
}
