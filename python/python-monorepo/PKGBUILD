pkgname=python-monorepo
pkgver=1
vcs=git
vcs_pkgname=monorepo

build() {
    :
}

package() {
    python_package
}

after_install() {
    mkdir -pv /var/lib/packages/{installed,uninstalled}
}

after_upgrade() {
    after_install
}
