pkgname=python-parso
pkgver=0.7.0
vcs=git
vcs_pkgname=parso
gittag=v${pkgver}
relmon_id=16689

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
