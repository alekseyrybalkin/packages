pkgname=python-dockerpty
pkgver=0.4.1
vcs=git
vcs_pkgname=dockerpty
gittag=v${pkgver}
relmon_id=7286

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
