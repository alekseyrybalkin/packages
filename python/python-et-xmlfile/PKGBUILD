pkgname=python-et-xmlfile
pkgver=1.0.1
vcs=mercurial
vcs_pkgname=et_xmlfile
hgtag=${pkgver}
relmon_id=15065

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
