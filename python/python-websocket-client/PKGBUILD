pkgname=python-websocket-client
pkgver=0.57.0
vcs=git
vcs_pkgname=websocket-client
gittag=v${pkgver}
relmon_id=7288

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
