pkgname=python-cffi
pkgver=1.14.0
vcs=mercurial
vcs_pkgname=cffi
hgtag=v${pkgver}
relmon_id=5536

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
