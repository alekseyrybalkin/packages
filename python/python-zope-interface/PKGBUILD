pkgname=python-zope-interface
pkgver=5.1.0
vcs=git
vcs_pkgname=zope.interface
gittag=${pkgver}

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
