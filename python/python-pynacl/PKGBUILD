pkgname=python-pynacl
pkgver=1.4.0
vcs=git
vcs_pkgname=pynacl
gittag=${pkgver}
relmon_id=12703

build() {
    :
}

package() {
    pip install \
        --no-deps \
        --no-build-isolation \
        --ignore-installed \
        --compile \
        --prefix=/usr \
        --root=${pkgdir} .
}
