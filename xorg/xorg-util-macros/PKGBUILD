pkgname=xorg-util-macros
pkgver=1.19.3
vcs=git
gittag=util-macros-${pkgver}
relmon_id=15037
updater_rules='lstrip_util-macros'

build() {
    ./autogen.sh --prefix=/usr --sysconfdir=/etc --localstatedir=/var --disable-static
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
