# required to build xorg-server
_pkgname=xkbcomp
pkgname=xorg-${_pkgname}
pkgver=1.4.4
vcs=git
gittag=${_pkgname}-${pkgver}
relmon_id=15018
updater_rules="lstrip_${_pkgname}"

build() {
    ./autogen.sh --prefix=/usr --sysconfdir=/etc --localstatedir=/var --disable-static
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
