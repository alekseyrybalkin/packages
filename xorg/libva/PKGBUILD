pkgname=libva
pkgver=2.7.0
vcs=git
gittag=${pkgver}
relmon_id=1752
updater_rules='skip_big_first'

build() {
    ./autogen.sh $XORG_CONFIG
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
