pkgname=lcms
pkgver=2.11
vcs=git
gittag=${pkgver}
relmon_id=9815

build() {
    ./autogen.sh --prefix=/usr \
        --disable-static \
        --libdir=/usr/lib \
        --without-tiff
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
