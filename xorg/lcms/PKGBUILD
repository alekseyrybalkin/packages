pkgname=lcms
pkgver=2.10
vcs=git
gittag=lcms${pkgver}
relmon_id=9815

build() {
    ./autogen.sh --prefix=/usr --disable-static --libdir=$LIBDIR
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
