pkgname=libepoxy
pkgver=1.5.5
vcs=git
gittag=${pkgver}
relmon_id=6090

build() {
    mkdir build
    meson --prefix=/usr . build
    ninja -C build
}

package() {
    DESTDIR=${pkgdir} ninja -C build install
}
