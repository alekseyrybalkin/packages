pkgname=harfbuzz
pkgver=2.6.5
vcs=git
gittag=${pkgver}
relmon_id=1299

build() {
    ./autogen.sh \
        --prefix=/usr \
        --with-icu=no \
        --libdir=$LIBDIR
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
