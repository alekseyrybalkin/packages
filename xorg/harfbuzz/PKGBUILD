pkgname=harfbuzz
pkgver=2.7.2
vcs=git
gittag=${pkgver}
relmon_id=1299

build() {
    ./autogen.sh \
        --prefix=/usr \
        --with-icu=no \
        --with-gobject \
        --libdir=$LIBDIR
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
