pkgname=libxcomposite
pkgver=0.4.5
vcs=git
gittag=libXcomposite-${pkgver}
relmon_id=1769

build() {
    ./autogen.sh --prefix=/usr --sysconfdir=/etc --localstatedir=/var --disable-static --libdir=/usr/lib
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
