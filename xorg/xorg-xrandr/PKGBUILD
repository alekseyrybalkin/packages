_pkgname=xrandr
pkgname=xorg-${_pkgname}
pkgver=1.5.1
vcs=git
gittag=${_pkgname}-${pkgver}
relmon_id=14957
updater_rules="lstrip_${_pkgname}"

build() {
    ./autogen.sh $XORG_CONFIG
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
