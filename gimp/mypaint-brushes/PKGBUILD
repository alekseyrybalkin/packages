pkgname=mypaint-brushes
pkgver=1.3.1
vcs=git
gittag=v${pkgver}
relmon_id=17096

build() {
    autoreconf -fiv
    ./configure --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
