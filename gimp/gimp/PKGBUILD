pkgname=gimp
pkgver=2.99.2
vcs=git
gittag=GIMP_${pkgver//\./_}
relmon_id=1161
updater_rules='skip_big_first'

build() {
    # checking for xgettext version (>= 0.19)... ./configure: line 18500: test: 21-dirty: integer expression expected
    sed -i -e "s/f4/f4 | sed 's\/\-dirty\/\/g'/g" configure.ac
    sed -i -e "/devel-docs/d" Makefile.am configure.ac

    ./autogen.sh \
        --prefix=/usr \
        --sysconfdir=/etc \
        --libexecdir=/usr/bin \
        --enable-mp \
        --enable-python \
        --enable-gimp-console \
        --with-libcurl \
        --disable-gtk-doc \
        --with-python=no \
        --with-lua=no \
        --with-javascript=no
    make
}

package() {
    make DESTDIR=${pkgdir} install
    ln -sv gimp-2.99 ${pkgdir}/usr/bin/gimp
}
