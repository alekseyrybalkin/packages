NON_FREE=1
#vcs=none
pkgname=zoom
pkgver=5.1.412382.0614
urls="https://zoom.us/client/${pkgver}/zoom_x86_64.pkg.tar.xz"
srctar="zoom_x86_64.pkg.tar.xz"
srcdir=${location}

build() {
    :
}

package() {
    cp -dpr --no-preserve=ownership opt usr ${pkgdir}
    rm -rf ${pkgdir}/usr/share/{pixmaps,mime,applications}
}
