pkgname=libsodium
pkgver=1.0.18
vcs=git
gittag=${pkgver}
relmon_id=1728

build() {
    DO_NOT_UPDATE_CONFIG_SCRIPTS=1 ./autogen.sh
    ./configure --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
