pkgname=ublock
pkgver=1.16.4.27
vcs=git
vcs_pkgname=ublock-for-firefox-legacy
gittag=firefox-legacy-${pkgver}
updater_rules="lstrip_firefox-legacy-"

build() {
    cd ../
    git clone `find_vcs_repo uassets` uAssets
    cd ${srcdir}
    ./tools/make-firefox-legacy.sh all
}

package() {
    mkdir -p ${pkgdir}/usr/lib/palemoon/browser/extensions
    emid="uBlock0@raymondhill.net"
    cp -r dist/build/*.xpi ${pkgdir}/usr/lib/palemoon/browser/extensions/${emid}.xpi
    find ${pkgdir} -type d -exec chmod 0755 {} \;
    find ${pkgdir} -type f -exec chmod 0644 {} \;
}
