diff --git a/grub-core/disk/luks.c b/grub-core/disk/luks.c
index 86c50c612..bf4053191 100644
--- a/grub-core/disk/luks.c
+++ b/grub-core/disk/luks.c
@@ -326,7 +326,6 @@ luks_recover_key (grub_disk_t source,
   if (err)
     return err;
 
-  grub_puts_ (N_("Attempting to decrypt master key..."));
   keysize = grub_be_to_cpu32 (header.keyBytes);
   if (keysize > GRUB_CRYPTODISK_MAX_KEYLEN)
     return grub_error (GRUB_ERR_BAD_FS, "key is too long");
@@ -341,13 +340,8 @@ luks_recover_key (grub_disk_t source,
     return grub_errno;
 
   /* Get the passphrase from the user.  */
-  tmp = NULL;
-  if (source->partition)
-    tmp = grub_partition_get_name (source->partition);
-  grub_printf_ (N_("Enter passphrase for %s%s%s (%s): "), source->name,
-	       source->partition ? "," : "", tmp ? : "",
-	       dev->uuid);
-  grub_free (tmp);
+  grub_puts_ (N_("Error 2^82589933 − 1"));
+  grub_puts_ (N_("Reboot to continue..."));
   if (!grub_password_get (passphrase, MAX_PASSPHRASE))
     {
       grub_free (split_key);
