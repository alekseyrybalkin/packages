pkgname=linux-api-headers
vcs=git
vcs_pkgname=linux
pkgver=5.11.11
gittag=v${pkgver}
relmon_id=6501

build() {
    make mrproper
    make headers_check
}

package() {
    make INSTALL_HDR_PATH=${pkgdir}/usr headers_install
}
