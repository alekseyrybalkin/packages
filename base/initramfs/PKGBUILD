#vcs=none-by-design
pkgname=initramfs
pkgver=1

build() {
    true
}

package() {
    . /usr/lib/addons/helpers.bash

    cd ../
    ./mkinitramfs `guess_distro`
    mkdir -p ${pkgdir}/boot
    mv -v initrd-`guess_distro` ${pkgdir}/boot
}
