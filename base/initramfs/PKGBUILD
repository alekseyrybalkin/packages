#vcs=none-by-design
pkgname=initramfs
pkgver=1

build() {
    true
}

package() {
    HOST_DISTRO=$(hostconf distro)

    cd ../
    ./mkinitramfs ${HOST_DISTRO}
    mkdir -p ${pkgdir}/boot
    mv -v initrd-${HOST_DISTRO} ${pkgdir}/boot
}
