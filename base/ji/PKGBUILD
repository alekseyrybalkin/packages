pkgname=ji
pkgver=54
vcs=git
gittag=v${pkgver}

build() {
    :
}

package() {
    mkdir -p ${pkgdir}/{etc,usr/bin,usr/lib/ji}
    cp exe ${pkgdir}/usr/bin/ji
    cp util.py ${pkgdir}/usr/lib/ji/
    cp conf.bash ${pkgdir}/etc/ji.conf.packaged
}
