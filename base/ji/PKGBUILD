pkgname=ji
pkgver=76
vcs=git
gittag=v${pkgver}

build() {
    :
}

package() {
    mkdir -p ${pkgdir}/usr/bin
    cp exe ${pkgdir}/usr/bin/ji
}

after_install() {
    mkdir -pv /var/lib/jinni/{installed,uninstalled}
}

after_upgrade() {
    after_install
}
