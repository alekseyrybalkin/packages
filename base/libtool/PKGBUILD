pkgname=libtool
pkgver=2.4.6
vcs=git
gittag=v${pkgver}
relmon_id=1741

build() {
    MAKEFLAGS=
    git clone -s `find_vcs_repo gnulib`
    ./bootstrap --skip-po
    ./configure --prefix=/usr
    make
}

package() {
    MAKEFLAGS=
    make DESTDIR=${pkgdir} install
}
