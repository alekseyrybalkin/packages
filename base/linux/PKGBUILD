pkgname=linux
vcs=git
pkgver=5.9.8
gittag=v${pkgver}
relmon_id=6501

build() {
    make mrproper
    cp -v ../config-x86_64 .config
    make
}

package() {
    HOST_DISTRO=$(hostconf distro)

    mkdir -pv ${pkgdir}/boot
    cp -v arch/x86_64/boot/bzImage ${pkgdir}/boot/vmlinuz-${HOST_DISTRO}
}
