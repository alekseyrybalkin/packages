pkgname=linux
vcs=git
pkgver=5.8.16
gittag=v${pkgver}
relmon_id=6501

build() {
    make mrproper
    cp -v ../config-x86_64 .config
    make
}

package() {
    . /usr/lib/addons/helpers.bash

    mkdir -pv ${pkgdir}/boot
    cp -v arch/x86_64/boot/bzImage ${pkgdir}/boot/vmlinuz-`guess_distro`
}
