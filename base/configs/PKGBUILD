pkgname=configs
pkgver=1
vcs=git

build() {
    ./bootstrap
}

package() {
    cp -av etc usr ${pkgdir}/

    chmod 440 ${pkgdir}/etc/sudoers
    chmod 600 ${pkgdir}/etc/crypttab
}

after_install() {
    domains --no-remote
}

after_upgrade() {
    after_install
}
