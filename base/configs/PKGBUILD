pkgname=configs
pkgver=1
vcs=git

build() {
    ./bootstrap
}

package() {
    ./bootstrap install ${pkgdir}
}

after_install() {
    if [ "$(hostconf distro)" == "jinni" ]; then
        domains --no-remote
    fi
}

after_upgrade() {
    after_install
}
