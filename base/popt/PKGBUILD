pkgname=popt
pkgver=1.17.0.1
vcs=bzr
bzrtag=jinni-${pkgver}
relmon_id=3689

build() {
    ./autogen.sh
    ./configure --prefix=/usr --disable-static
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
