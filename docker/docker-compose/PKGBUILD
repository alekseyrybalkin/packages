pkgname=docker-compose
pkgver=1.27.4
vcs=git
vcs_pkgname=compose
gittag=${pkgver}
relmon_id=6185

build() {
    sed -i 's/==/>=/g' requirements.txt
    sed -i "s/, < .*',$/',/" setup.py
    python-build
}

package() {
    python-package
}
