pkgname=btrfs-progs
pkgver=5.4.1
vcs=git
gittag=v${pkgver}
relmon_id=227

build() {
    ./autogen.sh
    ./configure --prefix=/usr --disable-zstd
    make
}

package() {
    make DESTDIR=${pkgdir} install install_python
}
