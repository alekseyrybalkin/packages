pkgname=libgee
pkgver=0.20.3
vcs=git
gittag=${pkgver}
relmon_id=1625

build() {
    NOCONFIGURE=1 ./autogen.sh
    ./configure --prefix=/usr --disable-static
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
