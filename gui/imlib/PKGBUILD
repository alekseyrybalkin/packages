pkgname=imlib
pkgver=1.7.0
vcs=git
vcs_pkgname=imlib2
gittag=v${pkgver}
relmon_id=21676

build() {
    ./autogen.sh --prefix=/usr --disable-static
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
