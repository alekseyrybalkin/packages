pkgname=poppler
pkgver=21.06.1
vcs=git
# b782d4be006c184c49c6b46bec1d23dd28f1683a
gittag=handmade-poppler-${pkgver}
relmon_id=3686

build() {
    mkdir build
    cd build

    cmake -DCMAKE_BUILD_TYPE=Release \
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DTESTDATADIR=$PWD/testfiles \
        -DENABLE_UNSTABLE_API_ABI_HEADERS=ON \
        -DENABLE_SPLASH=OFF \
        -DENABLE_LIBTIFF=OFF \
        -DENABLE_CMS=none \
        -DBUILD_GTK_TESTS=OFF \
        -DENABLE_LIBOPENJPEG=none \
        ..
    make
}

package() {
    cd build
    make DESTDIR=${pkgdir} install
    mv ${pkgdir}/usr/lib{64,}
}
