pkgname=nodejs
pkgver=13.6.0
vcs=git
gittag=v${pkgver}
relmon_id=8251

build() {
    ./configure --prefix=/usr \
        --shared-zlib \
        --shared-libuv \
        --with-intl=system-icu
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
