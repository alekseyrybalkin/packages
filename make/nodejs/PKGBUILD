pkgname=nodejs
pkgver=14.13.1
vcs=git
gittag=v${pkgver}
relmon_id=8251

build() {
    ./configure --prefix=/usr \
        --shared-zlib \
        --shared-openssl \
        --shared-libuv \
        --with-intl=small-icu
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
