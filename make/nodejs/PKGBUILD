pkgname=nodejs
pkgver=12.8.0
vcs=git
gittag=v${pkgver}
relmon_id=8251

build() {
    ./configure --prefix=/usr \
        --shared-zlib \
        --shared-libuv \
        --with-intl=small-icu
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
