pkgname=yelp-tools
pkgver=3.38.0
vcs=git
gittag=${pkgver}
relmon_id=8420
updater_rules='skip_big_third'

build() {
    ./autogen.sh --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
