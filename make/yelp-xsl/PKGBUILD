pkgname=yelp-xsl
pkgver=3.38.0
vcs=git
gittag=${pkgver}
relmon_id=13164
updater_rules="skip_odd_second"

build() {
    ./autogen.sh --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
