pkgname=nasm
pkgver=2.15.02
vcs=git
gittag=nasm-${pkgver}
relmon_id=2048

build() {
    ./autogen.sh
    ./configure --prefix=/usr
    make
    make manpages
}

package() {
    make DESTDIR=${pkgdir} install
}
