pkgname=sdl2_mixer
pkgver=2.0.4
vcs=mercurial
hgtag=release-${pkgver}
relmon_id=4782

build() {
    ./configure --disable-static --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
