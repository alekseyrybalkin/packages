pkgname=gtk-doc
pkgver=1.33.0
vcs=git
gittag=${pkgver}
relmon_id=13140
updater_rules='lstrip_gtk_doc'

build() {
    mkdir build
    meson --prefix=/usr -D libexecdir=/usr/lib -D sysconfdir=/etc . build
    ninja -C build
}

package() {
    DESTDIR=${pkgdir} ninja -C build install
}
