pkgname=libevent
pkgver=2.1.11
vcs=git
gittag=release-${pkgver}-stable
relmon_id=1606

build() {
    autoreconf -fi
    ./configure --prefix=/usr --sysconfdir=/etc
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
