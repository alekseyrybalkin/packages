pkgname=asciidoc
pkgver=9.0.3
vcs=git
gittag=${pkgver}
relmon_id=13262

build() {
    autoconf
    ./configure --prefix=/usr --sysconfdir=/etc
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
