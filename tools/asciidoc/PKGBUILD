pkgname=asciidoc
pkgver=9.0.0rc2
vcs=git
gittag=${pkgver}
relmon_id=13262
updater_rules="no_alpha_skips"

build() {
    autoconf
    ./configure --prefix=/usr --sysconfdir=/etc
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
