pkgname=asciidoc
pkgver=9.0.0rc2
vcs=git
gittag=${pkgver}
relmon_id=13262

build() {
    autoconf
    ./configure --prefix=/usr --sysconfdir=/etc
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
