pkgname=scons
pkgver=3.1.2
vcs=git
gittag=${pkgver}
relmon_id=4770

build() {
    python-build
}

package() {
    python bootstrap.py
    tar xvf build/dist/scons-${pkgver}.tar.gz
    cd scons-${pkgver}
    python-package

    mkdir -p ${pkgdir}/usr/share
    mv ${pkgdir}/usr/{,share/}man
}
