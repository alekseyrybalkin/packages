pkgname=breezy
SKIP_ARCH_CHECK=1
pkgver=3.0.1
vcs=git
gittag=brz-${pkgver}

build() {
    python setup.py build
}

package() {
    python setup.py install --prefix=/usr --root=${pkgdir} --install-data="usr/share" --optimize=1
}
