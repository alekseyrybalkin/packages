pkgname=breezy
pkgver=3.1.0.4
vcs=git
gittag=$(echo ${pkgver} | rev)
gittag=$(echo ${gittag/\./-} | rev)
relmon_id=45817

build() {
    python-build
}

package() {
    python-package

    mkdir -p ${pkgdir}/usr/share
    mv ${pkgdir}/usr/{,share/}man
}
