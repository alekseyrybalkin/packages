pkgname=swig
pkgver=4.0.2
vcs=git
gittag=rel-${pkgver}
relmon_id=4919

build() {
    ./autogen.sh
    ./configure --prefix=/usr \
        --without-pcre
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
