pkgname=serf
pkgver=1.3.9
vcs=git
gittag=${pkgver}
relmon_id=1720

build() {
    patch -p1 -i ../scons-python3.patch
    scons PREFIX=/usr
}

package() {
    mkdir -p ${pkgdir}/usr
    scons PREFIX=${pkgdir}/usr install
    chmod 644 ${pkgdir}/usr/include/serf-1/*
}
