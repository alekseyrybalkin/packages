pkgname=libxslt
pkgver=1.1.34
vcs=git
gittag=v${pkgver}
relmon_id=13301

build() {
    ./autogen.sh --prefix=/usr \
        --disable-static \
        --with-python=/usr/bin/python
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
