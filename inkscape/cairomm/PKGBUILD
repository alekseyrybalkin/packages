pkgname=cairomm
pkgver=1.12.2
vcs=git
gittag=v${pkgver}
relmon_id=7959
relmon_rules='skip_odd_second'

build() {
    ./autogen.sh --prefix=/usr --enable-maintainer-mode
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
