pkgname=libsigc++
pkgver=2.10.2
vcs=git
gittag=${pkgver}
# we use 2.x but 3.x is released
#relmon_id=7961
#updater_rules='skip_odd_second'

build() {
    ./autogen.sh --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
