pkgname=libsigc++
pkgver=2.10.3
vcs=git
gittag=${pkgver}
relmon_id=7961
updater_rules='skip_odd_second'

build() {
    ./autogen.sh --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
