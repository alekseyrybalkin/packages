pkgname=inkscape
pkgver=1.0.beta2
vcs=git
gittag=INKSCAPE_${pkgver//\./_}
gittag=${gittag^^}
relmon_id=1381

build() {
    mkdir build
    cd build
    export CXXFLAGS="${CXXFLAGS} -fpermissive"
    cmake .. \
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_BUILD_TYPE=RELEASE \
        -DENABLE_POPPLER=OFF \
        -DENABLE_POPPLER_CAIRO=OFF \
        -DWITH_IMAGE_MAGICK=OFF \
        -DWITH_GRAPHICS_MAGICK=OFF \
        -DWITH_LIBCDR=OFF \
        -DWITH_LIBVISIO=OFF
    make
}

package() {
    cd build
    make DESTDIR=${pkgdir} install
}
