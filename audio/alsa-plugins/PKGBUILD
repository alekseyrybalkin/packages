pkgname=alsa-plugins
pkgver=1.2.2
vcs=git
gittag=v${pkgver}
relmon_id=41

build() {
    autoreconf -fi
    ./configure --prefix=/usr --sysconfdir=/etc
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
