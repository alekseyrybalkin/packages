pkgname=libtheora
pkgver=1.1.1.0.1
vcs=git
# e5d205bfe849f1b41f45b91a0b71a3bdc6cd458f
gittag=jinni-v${pkgver}
relmon_id=11793

build() {
    ./autogen.sh
    ./configure --prefix=/usr --disable-static
    make
}

package() {
    make DESTDIR=${pkgdir} install
}
