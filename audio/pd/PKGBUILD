pkgname=pd
pkgver=0.51.1
vcs=git
vcs_pkgname=pure-data
gittag=jinni-${pkgver}

build() {
    ./autogen.sh
    ./configure --prefix=/usr
    make
}

package() {
    make DESTDIR=${pkgdir} install
    rm ${pkgdir}/usr/lib/pd/bin/pd
    ln -sv /usr/bin/pd ${pkgdir}/usr/lib/pd/bin/pd
}
