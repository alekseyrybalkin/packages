`kiin check-buildorder`
`sudo kiin check-system-integrity`
`kiin check-updates`
`find /var/lib/kiin -name "kiin*xz" | grep -v installed`
check if kiin `rebuild-world` looks into all folders under /var/lib/kiin
# check and update TODO
