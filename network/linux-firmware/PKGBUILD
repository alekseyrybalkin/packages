NON_FREE=1
pkgname=linux-firmware
pkgver=20191215
ARCH_VERSION=20191220.6871bff
vcs=git
gittag=${pkgver}

build() {
    :
}

package() {
    mkdir -pv ${pkgdir}/usr/lib/firmware
    cp -v ./iwlwifi-6*.ucode ${pkgdir}/usr/lib/firmware/
    cp -v ./iwlwifi*7265* ${pkgdir}/usr/lib/firmware/
}
